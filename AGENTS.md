# AGENTS.md

이 문서는 이 저장소에서 작업하는 Codex와 자동화 에이전트가 따라야 할 프로젝트 지침입니다. 저장소 전체에 적용합니다.

## 1. 제품 맥락

이 프로젝트는 처음 교회를 알아보는 사람을 주요 사용자로 삼는 교회 홈페이지입니다.

제품의 가장 중요한 역할은 교회 내부의 모든 정보를 한곳에 모으는 것이 아니라, 처음 방문한 사람이 교회의 성격과 분위기를 이해하고 실제 방문에 필요한 정보를 편안하게 얻도록 돕는 것입니다.

핵심 사용자 흐름은 다음과 같습니다.

```text
유입 → 교회 이해 → 분위기 탐색 → 예배·위치 확인 → 방문 또는 문의
```

상세한 제품 요구사항은 `docs/PRODUCT_SPEC.md`를 기준으로 합니다.

## 2. 우선순위

기능이나 화면을 설계할 때 다음 순서를 우선합니다.

1. 처음 방문자의 이해와 안심
2. 예배 시간과 위치 같은 방문 정보의 접근성
3. 실제 공동체의 분위기를 보여주는 콘텐츠
4. 모바일 사용성 및 접근성
5. 관리자의 간편하고 일관된 콘텐츠 운영
6. 기존 교인을 위한 부가 정보

내부 교인에게만 필요한 기능이 첫 방문자 경험을 방해하지 않도록 합니다.

## 3. UX 및 콘텐츠 원칙

- 내비게이션의 최상위 항목은 가능하면 5개 안팎으로 유지합니다.
- 홈 화면은 게시판 모음이 아니라 하나의 이야기처럼 읽히게 구성합니다.
- 교회 용어를 당연하게 사용하지 말고, 처음 접하는 사람도 이해할 수 있는 말로 씁니다.
- “은혜로운 공동체” 같은 추상적인 표현만 사용하지 않습니다. 실제 활동, 사진, 시간, 장소처럼 판단 가능한 근거를 함께 제공합니다.
- 사용자가 예배 시간, 지도, 전화번호를 찾기 위해 여러 단계를 거치지 않게 합니다.
- 새가족 등록을 지나치게 강요하지 않습니다. 먼저 정보를 충분히 제공하고 문의나 방문 신청을 선택할 수 있게 합니다.
- 실제 교회 및 공동체 사진을 우선하며, 장식 목적의 스톡 이미지는 최소화합니다.
- 주보 PDF만 제공하지 말고 핵심 공지와 일정은 웹 화면에서도 읽을 수 있게 설계합니다.

## 4. 시각 디자인 원칙

- 차분하고 따뜻하며 현대적인 인상을 지향합니다.
- 넓은 여백, 명확한 타이포그래피, 큰 실제 사진을 중심으로 구성합니다.
- 카드, 배너, 바로가기 버튼을 습관적으로 반복하지 않습니다.
- 한 화면에서 강조 색상과 주요 행동을 과도하게 늘리지 않습니다.
- 장식보다 정보의 위계와 콘텐츠의 진정성을 우선합니다.
- 데스크톱뿐 아니라 모바일 화면을 처음부터 함께 설계합니다.

구체적인 색상, 글꼴, 로고 규칙은 브랜드 방향이 확정된 후 별도 디자인 토큰으로 관리합니다.

## 5. 정보 구조 기본안

기본 최상위 메뉴는 다음과 같습니다.

- 교회 소개
- 예배 안내
- 교회 이야기
- 설교
- 처음 오셨나요?

주보, 공지, 오시는 길, 문의는 사용 빈도와 맥락에 맞춰 홈 또는 관련 메뉴에 연결합니다. 요구사항이 생겼다는 이유만으로 최상위 메뉴를 추가하지 말고, 기존 사용자 흐름 안에 배치할 수 있는지 먼저 검토합니다.

## 6. 관리자 경험

- 관리자는 페이지 레이아웃을 직접 조립하기보다 정해진 콘텐츠 양식에 입력하는 방식을 우선합니다.
- 콘텐츠 유형별로 필수 필드와 선택 필드를 명확히 구분합니다.
- 게시 전 미리보기, 임시 저장, 게시 상태 관리를 고려합니다.
- 이미지 업로드 시 비율, 용량, 대체 텍스트를 안내합니다.
- 날짜가 지난 행사와 오래된 공지는 자동으로 구분되거나 보관될 수 있어야 합니다.
- 삭제보다 비공개 또는 보관처럼 복구 가능한 상태 변경을 우선합니다.

## 7. 구현 원칙

### 7.1 확정된 개발 스택

- 공개 웹은 `Next.js` App Router와 `React`, `TypeScript`로 구현합니다. 방문자 페이지는 검색 엔진이 읽을 수 있도록 서버 렌더링 또는 정적 생성 기반으로 제공합니다.
- 기존 정적 데모의 시각 디자인과 콘텐츠 흐름을 유지하며, UI는 재사용 가능한 React 컴포넌트로 이전합니다.
- 스타일은 전역 토큰과 기본 요소에만 전역 SCSS를 사용하고, 컴포넌트별 스타일은 SCSS CSS Modules(`*.module.scss`)로 분리합니다. `sass` 외에 스타일링 의존성은 추가하지 않습니다.
- 콘텐츠 관리와 관리자 화면은 Node.js 기반 Headless CMS인 `Directus`를 사용합니다. 초기 데이터베이스는 `SQLite`로 운영합니다.
- 공개 웹은 SQLite에 직접 접근하거나 SQL을 작성하지 않고, Directus REST API 또는 SDK를 통해 콘텐츠를 조회합니다.
- Directus의 공개 읽기 권한은 `published` 콘텐츠로 제한합니다. `draft`와 `archived` 콘텐츠, 사용자 정보, 관리자 설정은 공개 API에 노출하지 않습니다.
- 관리자 로그인은 Directus의 Google OpenID Connect를 사용합니다. OAuth 인증과 Directus 역할·정책 기반 인가를 분리하고, 승인된 계정에만 최소 권한을 부여합니다.
- 콘텐츠 API와 관리 기능은 Directus가 담당합니다. 방문 문의나 외부 서비스 연동처럼 사이트 고유의 서버 처리가 필요할 때만 Next.js Route Handler를 추가합니다.
- 공개 경로는 `/`, `/about`, `/worship`, `/stories`, `/stories/[slug]`, `/sermons`, `/sermons/[slug]`, `/news`, `/news/[slug]`처럼 명시적으로 구성합니다. React의 클라이언트 전환 여부와 별개로 모든 공개 콘텐츠에는 고유 URL을 제공합니다.
- 페이지별 메타데이터, canonical URL, Open Graph 이미지, `robots.txt`, `sitemap.xml`, 구조화 데이터는 Next.js에서 관리합니다.

배포, 도메인, HTTPS, 리버스 프록시, 컨테이너 운영 방식은 별도 배포 단계에서 결정합니다.

구현이 시작된 후에는 다음을 지킵니다.

- 기존 프로젝트 구조와 스타일을 먼저 확인하고 일관되게 작업합니다.
- 불필요한 의존성을 추가하지 않습니다.
- 반복되는 UI와 콘텐츠 모델은 재사용 가능한 단위로 만듭니다.
- 반응형 레이아웃, 키보드 조작, 의미 있는 HTML, 이미지 대체 텍스트를 기본 요구사항으로 취급합니다.
- 중요한 방문 정보는 JavaScript가 늦게 로드되더라도 가능하면 읽을 수 있게 합니다.
- 외부 영상과 지도 임베드는 성능 및 개인정보 영향을 고려해 지연 로딩합니다.
- 실제 교인이나 방문자의 개인정보를 샘플 데이터에 넣지 않습니다.

## 8. 작업 전후 확인사항

작업 전:

- `README.md`와 `docs/PRODUCT_SPEC.md`를 확인합니다.
- 관련 화면과 콘텐츠 모델의 기존 구현 여부를 확인합니다.
- 요청이 첫 방문자 경험에 미치는 영향을 검토합니다.

작업 후:

- 변경한 흐름을 모바일과 데스크톱 관점에서 확인합니다.
- 빈 상태, 로딩 상태, 오류 상태가 필요한지 확인합니다.
- 문구가 교회 내부자만 이해할 수 있는 표현인지 점검합니다.
- 기능 또는 정보 구조가 달라졌다면 관련 기획 문서도 함께 갱신합니다.
- 가능한 범위에서 테스트, 린트, 빌드를 실행하고 결과를 기록합니다.

## 9. 범위 관리

현재 MVP에는 교인 로그인, 출석 관리, 온라인 헌금, 소그룹 관리, 실시간 채팅을 포함하지 않습니다. 이런 기능을 추가할 때는 방문자 중심 홈페이지와 분리된 서비스가 더 적합한지 먼저 검토합니다.

불명확한 세부 사항은 제품 목표에 맞는 합리적인 기본값으로 진행할 수 있습니다. 다만 브랜드 정체성, 실제 예배 정보, 개인정보 수집, 외부 서비스 계약처럼 결과를 크게 바꾸는 사항은 임의로 확정하지 않습니다.

## 10. 커밋 및 이슈 기록

- 모든 커밋은 Conventional Commits 형식의 제목으로 작성합니다. 예: `feat: ...`, `fix: ...`, `refactor: ...`, `docs: ...`.
- 커밋 제목 아래에는 변경 목적, 핵심 변경 사항, 검증 결과를 담은 본문을 반드시 작성합니다. 본문을 생략하지 않습니다.
- 특정 GitHub 이슈와 관련된 작업은 커밋 본문에서 해당 이슈 번호를 참조합니다. 예: `Refs: #15`.
- 특정 GitHub 이슈 작업을 시작하기 전에는 해당 이슈 전용 브랜치를 새로 만듭니다. 브랜치 이름은 작업 성격과 이슈 번호가 드러나게 작성합니다. 예: `refactor/issue-16-content-modules`.
- 이슈 작업 커밋은 해당 브랜치에만 쌓고 원격 브랜치까지 푸시합니다. `main` 병합은 사용자 코드 리뷰와 명시적인 통합 지시 후에만 수행합니다.
